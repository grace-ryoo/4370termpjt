<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Recipe Share</title>
    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
    <!-- Navbar -->
    {{> components/navbar }}

    <!-- Cookbook Header -->
    <header class="text-center py-8">
        <h1 class="text-4xl font-bold text-[#90af7b]">Chronicles to Cook</h1>
    </header>

    <!-- Saved Recipes Grid -->
    <section class="max-w-6xl mx-auto px-6 py-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
            {{#cookbook}}
                <div class="bg-white rounded-lg overflow-hidden shadow-md hover:shadow-lg transition-shadow">
                    <img src="{{imageUrl}}" alt="{{title}}" class="w-full h-48 object-cover">
                    <div class="p-4">
                        <h2 class="text-xl font-semibold text-[#90af7b] mb-2">{{title}}</h2>
                        <p class="text-gray-500 mb-4">By {{author}}</p>
                        <div class="flex justify-between text-sm text-gray-400">
                            <span>{{likes}} likes</span>
                            <span>{{category}}</span>
                        </div>
                    </div>
                </div>
            {{/cookbook}}
        </div>

        {{^cookbook}}
            <div class="text-center text-gray-500 mt-10">
                <p>No recipes saved yet. Start exploring and bookmarking!</p>
            </div>
        {{/cookbook}}
    </section>

</body>
</html>
